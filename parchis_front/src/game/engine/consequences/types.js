export const CONSEQUENCE_RESOLUTION_STATUS = Object.freeze({
  RESOLVED: 'resolved',
  DECISION_REQUIRED: 'decisionRequired',
  STOPPED: 'stopped',
});

export const CONSEQUENCE_ITEM_TYPES = Object.freeze({
  REWARD: 'reward',
  OPTIONAL_ABILITY_ACTIVATION: 'optionalAbilityActivation',
});
